
section ↦ “Tree structure”

makeLeaf a ↦ Leaf
	Leaf ↦ a

makeBranch a b ↦ Branch
	Branch ↦ a b


map f t ↦
	if (≔ head t Leaf) (≔ makeLeaf (≔ f (≔ t)))
	if (≔ head t Tree) (≔ makeBranch (≔ map f (right ≔ t)) (≔ map f right))

